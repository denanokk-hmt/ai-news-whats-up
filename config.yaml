schedule:
  times: ["07:00", "19:00"]
  timezone: "Asia/Tokyo"

gemini:
  model: "gemini-3.1-pro-preview"

output:
  local_dir: "output"

notifications:
  slack:
    enabled: false
    webhook_url: "${SLACK_WEBHOOK_URL}"
